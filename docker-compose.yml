services:
  redis:
    image: redis
    container_name: redis
    restart: always
    networks:
      - bot-network

  bot:
    container_name: bot
    build: .
    depends_on:
      - redis
    env_file:
      - .env
    volumes:
      - bot_data:/app/data

    restart: always
    networks:
      - bot-network
        
networks:
  bot-network:
    driver: bridge

volumes:
  bot_data:
