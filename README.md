# 📘 **Sibsau Timetable Bot**

Telegram-бот для просмотра расписания Сибирского государственного университета науки и технологий имени академика М. Ф. Решетнёва.

Бот предоставляет удобный доступ к актуальному расписанию занятий, поддерживает персонализацию по группе и подгруппе, а также отправляет автоматические уведомления пользователю в выбранное время.

---

## 🚀 **Функциональность**

* 📅 Получение расписания:

  * на **сегодня**
  * на **завтра**
  * на **неделю**
* 🧩 Персонализация:

  * выбор учебной группы
  * выбор подгруппы
* 🔔 Автоматические уведомления:

  * ежедневная отправка расписания
  * пользователь сам выбирает время
* ♻️ Автообновление расписания с сайта СибГУ (парсинг через BeautifulSoup)
* ⚙️ Использование FSM + Redis
* 🐳 Полностью работает в Docker

---

## 🛠 **Стек технологий**

* **Python 3.12**
* **Aiogram 3** — Telegram Bot API
* **APScheduler** — планировщик задач
* **BeautifulSoup4** — парсинг HTML-страниц
* **SQLite** — основная база данных
* **Redis** — состояние FSM и кэш
* **Poetry** — управление зависимостями
* **Docker / docker-compose** — деплой

---

## 📦 **Структура проекта**

```
project/
│── app/
│   ├── handlers/
│   ├── keyboards/
│   ├── notify/ 
│   ├── db/
│   ├── fsm/
│   ├── filters/
│   ├── client/
│   └── main.py
│── data/
│── docker-compose.yml
│── Dockerfile
│── pyproject.toml
│── .env.example
│── README.md
```

---

## ⚙️ **Запуск через Docker Compose**

### 1. Клонировать репозиторий

```bash
git clone https://github.com/denisbakulin/timetable_bot.git
cd sibsau-timetable-bot
```

### 2. Создать файл `.env`

```bash
cp .env.example .env
```

Установите переменные окружения

### 3. Запуск всего проекта

```bash
docker compose up --build -d
```

---

## 📝 **Основные команды бота**

* `/start` — начало работы, выбор группы
* `/menu` — главное меню
* `/today` — расписание на сегодня
* `/tomorrow` — расписание на завтра
